# Read automatically by `docker compose` for variable substitution in docker-compose.yml.
# Copy to .env and adjust if you want anything other than the defaults.
DB_PASSWORD=rootpass
DB_NAME=store_ratings
JWT_SECRET=replace_with_a_long_random_string
